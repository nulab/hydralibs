{
  "name": "hydralibs",
  "version": "0.2.13",
  "scripts": {
    "build": "turbo build",
    "check": "npm run build && npm run lint && npm test",
    "lint": "turbo lint",
    "lint:fix": "turbo lint:fix",
    "test": "turbo test"
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "turbo": "^1.8.3"
  },
  "workspaces": [
    "packages/*"
  ]
}
