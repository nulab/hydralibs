{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "lib/**"]
    },
    "lint": {},
    "lint:fix": {},
    "test": {
      "inputs": ["src/**/*.ts", "*.ts"]
    }
  }
}
